/* condition
 if condition */

// syntex
if (condition) {
  // code to execute
}
// if the condition is true than the code will execute. otherwise return

// if else condition

// syntex
if (condition) {
  // code to execute(code 1)
} else {
  // code to execute(code 2)
}
// if the condition is true then code one will run. otherwise code 2 will run

// if else if condition

// syntex
if (condition1) {
  // code to execute (code - 1)
} else if (condition2) {
  // code to execute (code - 2)
} else {
  // code to execute (code - 3)
}
// if condition1 is true then code - 1 will execute, if condition2 is true then code - 2 will execute, otherwise code - 3 will exexute

// loop
// white loop

// syntex
/* declear variable

while(condition) {
    // code to execute
    // update the variable
} */
// example
let i = 0;
while (i <= 10) {
  console.log(i);
}

// for loop

// syntex
/* for(declear variable; condition; update variable) {
    // code to execute
} */

// example
for (i = 0; i <= 10; i++) {
  console.log(i);
}

// Mathematical operator
// '+' = Addition
// '-' = Substraction
// '*' = Multiplication
// '/' = Divition
// '%' = Modulas

let a = 10;
let b = 5;

// Addition
let addition = a + b;

// Substraction
let sub = a - b;

// Multiplication
let multi = a * b;

// Divition
let division = a / b;

// Modulas
let modulas = a % b;

// Conditional oprator
// > less than
// < greater than
// '&&' AND operator
// '||' OR operator
// '==' EQUAL TO operator
// '!=' NOT EQUAL TO operator

// AND OPERATOR
// we can put two or more condition for some code to execute. if we put AND operator all the condition must true

let number = 12;
if (number < 20 && number > 5) {
  // code to execute
}

// OR OPERATOR
// we can put two or more condition for some code to execute. if we put OR operator one of the condition must true

if (number < 11 || number > 5) {
  // code to execute
}
